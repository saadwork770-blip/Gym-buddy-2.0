/* ============================================================================
   GymBuddy — data/brand-photos.js
   ----------------------------------------------------------------------------
   GENERATED by tools/import-photos.js. Do not edit by hand.

   Which exercises each brand has its own photographs for. The app is a static
   site with no server to ask, so it cannot test whether a file exists before
   asking for it — and a missing image would be a 404 in everyone's console and
   a broken frame on the page. This list is the answer instead: an id in here
   is served from assets/photos/<brand>/, and anything not in here falls back
   to the shared set.

   A brand with an empty list ships no photography of its own, which stays the
   honest state until somebody photographs their own gym.
   ============================================================================ */

const BRAND_PHOTOS = {
  /* Empty on purpose. Technogym's own product photography is theirs and is
     not licensed for redistribution, so nothing brand-specific ships here.
     Photograph your gym's machines and tools/import-photos.js --brand
     technogym will fill this in. */
  "technogym": [],
};
